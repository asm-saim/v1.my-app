import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Home",
  description: "...",
};

const home = () => {
  return (
    <div>
      <h1>Home section </h1>
    </div>
  );
};

export default home;
